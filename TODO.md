- [x] Align `admin.css` selectors to match `pages/profile_admin.php` class names (`.panel`, `.data-table`, `.field`, `.member-card`, etc.)
- [x] Reuse existing CSS variables from `style.css` for consistent theme (green, border-light, shadows, etc.)
- [x] Add dark-mode overrides for admin components
- [ ] Verify admin UI visually (tables, forms, buttons, cards, badges)


